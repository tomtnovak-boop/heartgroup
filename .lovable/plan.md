

# Revert to Before Coach Email Popover Change

The page stopped loading after the coach email popover was added. You can revert to the version before that change using the built-in history feature.

Click the revert button below the AI message **just before** the "coach name clickable to show email" change — that was the message that fixed the coach creation flow.

Alternatively, use the History tab:

