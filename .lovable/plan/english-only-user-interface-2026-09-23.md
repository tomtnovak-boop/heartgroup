# English-only user interface

## Scope

- Audit every TypeScript and TSX file under `src/` for text visible to users.
- Translate German buttons, labels, headings, placeholders, tooltips, dialogs, toasts, validation messages, dashboards, waiting screens, and legal pages into natural English.
- Preserve code identifiers, routes, data values, database fields, control flow, and console logging exactly as-is.

## Verification

- Re-scan `src/` for German characters and common German UI terms.
- Check the generated preview build and inspect representative authentication, admin, coach, display, and legal screens.
